# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Application binaries
nats-k8s-auth
/nats-k8s-oidc-callout
nats-k8s-oidc-callout-*
/server

# Build output directory
out/

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
.idea/
# .vscode/

# Local development secrets and credentials
# NEVER commit these files - they contain sensitive cryptographic material
local-dev/signing.key          # Account signing key (SA...)
local-dev/*.key                # Any other key files
local-dev/*.creds              # NATS credentials files
local-dev/kubeconfig.yaml      # Kubernetes credentials
local-dev/jwks.json            # JWKS (may contain sensitive key material)
local-dev/*.pem                # Certificate files
local-dev/*.p12                # Certificate bundles
local-dev/*.pfx                # Certificate bundles
